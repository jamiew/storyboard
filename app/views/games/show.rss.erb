xml.instruct! :xml, :version=>"1.0" 
xml.rss(:version=>"2.0"){
  xml.channel{
    xml.title("StoryBord.org")
    xml.link("http://storybord.org")
    xml.description("Image board for cool people only")
    xml.language('en-us')
      for game in @games
        xml.item do
          xml.title(post.title)
          #xml.description(post.html_content)      
          xml.author("Your Name Here")               
          #xml.pubDate(post.created_on.strftime("%a, %d %b %Y %H:%M:%S %z"))
          #xml.link(post.link)
          #xml.guid(post.link)
        end
      end
  }
}